import styled from "styled-components";


export const UserIconStyle = styled.header`
    display: inline-block;

    cursor: pointer; 

    svg {
        cursor: pointer;
        position: relative;

        top: 25px;
        right: 15px;

        color: var(--white);
    }

    p {
        text-align: center;

        margin: 22.5px 50px 20px 0;

        font-family: "Roboto Light", serif;
        color: var(--white);
    }
`